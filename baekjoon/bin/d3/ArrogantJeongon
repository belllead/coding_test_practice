A1, A2, ... AN
1 <= i < j <= N
if Ai * Aj => 단조 증가 then
	Array에 추가
Array 내 최댓값 출력

main():
	입력
		양의 정수 개수 N
		양의 정수 A1 ~ AN 배열 A
	최댓값 변수 max -1
	for 1 <= i < j <= N 인 양의 정수 i, j
		int temp = Ai * Aj
		if max < temp
			temp가 단조증가하는가?
				맞으면 max = temp
		
	max 출력
	
	
단조증가(int a):
	while a != 0
		int digit = a의 일의자리
		a = a / 10
		if a의 일의자리 > digit
			return false
	return true